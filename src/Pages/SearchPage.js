import React, {useState} from 'react';
import SearchBar from "../Components/SearchBar";

const SearchPage = () => {

    return (
        <div>
            <SearchBar/>
        </div>
    );
};

export default SearchPage;